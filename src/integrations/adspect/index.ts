/**
 * Adspect Integration — Public API
 *
 * Import from here, not from internal modules directly.
 */

export { AdspectClient } from "./client";
export type { AdspectConfig } from "./client";
export type {
  AdspectStream,
  AdspectFunnelRow,
  AdspectFunnelParams,
} from "./types";
